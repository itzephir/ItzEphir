{
  "name": "itzephir-deploy-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:visual": "node --test tests/*.visual.mjs"
  },
  "devDependencies": {
    "playwright-core": "1.62.1",
    "pngjs": "7.0.0"
  }
}
